# Example 1 - Private DNS zone
private_dns_zones = {
  "psql-test.postgres.database.azure.com" = {
    resource_group_name = "rg-test"
  }
}

# Example 2 - Private DNS zone w/ a VNet link
private_dns_zones = {
  "psql-test.postgres.database.azure.com" = {
    resource_group_name = "rg-test"
    virtual_network_links = {
      vnet-test-01 = {
        resource_group_name = "rg-test"
      }
    }
  }
}
